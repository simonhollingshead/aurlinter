# Folder naming
readonly DLDIR=prep
readonly RESULTDIR=res
readonly EXTDIR=ext

# Define the absolute path from the guest to the directory downloads and results will be in, and the relative location of it on the host as compared to this script.
readonly HOSTPATH=/home/aurlinter/aurlinter
readonly VMHOSTPATH=/smb/francium
readonly RELHOSTPATH=.

# To stop requests across the SMB boundary, designate a working directory inside the guest.  Also designate somewhere in TMP we can put the results.
readonly VMWORKDIR=/home/aur
readonly VMTMPDIR=/tmp/vm
readonly VMUSER=aur
readonly NSPAWN_CONTAINER=/vm